Refactor the weekly scheduling view to focus on planning only and remove employee weekly summaries.

1. Remove employee weekly totals

Remove the “Weekly” column for individual employees.

Remove any weekly actual / worked hours summary per employee.

This screen should not show employee weekly totals.

2. Keep department-level weekly summary only

In the “Resources” row per department:

Keep per-day:

Forecast

Scheduled

Delta

Add at the far right of the Resources row:

Weekly Forecast

Weekly Scheduled

Weekly Delta

Display it as a compact weekly summary block aligned with the Resources row.

3. Remove redundant department total row

Remove the “Department Total” row at the bottom if it duplicates Scheduled hours already shown in Resources.

Do not duplicate scheduled totals in multiple places.

4. Clean up resource indicators

Remove text labels like “ok / deficit”.

Keep only color-based delta indication (green/red) and numeric delta value.

Avoid redundant indicators.

5. Preserve layout logic

Keep Employees and Shifts layout unchanged.

Keep Open Shifts section unchanged.

Do not introduce additional summary rows.

Keep the layout stable and clean.