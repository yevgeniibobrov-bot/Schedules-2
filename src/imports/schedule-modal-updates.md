Employee behavior:
If the user opens the modal from an employee cell in the schedule grid,
automatically preselect that employee.

Example:
Працівник: Ольга Ковальчук

Only allow "Відкрита зміна" if the modal is opened from the "Open shifts" row.

Shift type selector:

Replace large card layout with compact radio selection:

Тип зміни

● Звичайна
○ Біржа змін
○ Запропонувати працівнику

Show "Запропонувати працівнику" only if an employee is selected.

Time input:

Remove the separate "Час зміни" section.
Time should be defined only inside shift segments.

Rename:

"Часові блоки"

to

"Сегменти зміни"

Remove analytics from the segment block:

Delete
Прогн / Запл / Залишок

These metrics belong in the schedule grid, not in the creation modal.

Breaks:

Keep break duration control (30 хв),
but add a break start time selector.

Example:

Перерва
30 хв
Початок: 12:00

Timeline:

Use the timeline only as a visual representation of segments and breaks.

Validation:

Keep the section "Перевірка".

Footer:

Keep only two buttons:

[ Скасувати ]
[ Створити ]