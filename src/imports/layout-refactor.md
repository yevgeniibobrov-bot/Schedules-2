Employee Rows:

Each employee row must have fixed height.

Shift cards must be vertically centered inside the row.

Cards must not extend above or below row boundaries.

Add proper vertical padding inside rows (equal top and bottom spacing).

Fix Shift Card Positioning:

Ensure cards snap exactly to time grid columns.

Prevent visual floating above row.

Maintain consistent border radius and shadow.

No overlap into department headers.

Resources Row:

Change background to very light neutral tone.

Remove heavy tint.

Make it visually lighter than employee rows.

Ensure it does not compete with shift cards.

Keep forecast/scheduled/delta readable but subtle.

Grid Lines:

Hour lines = subtle solid lines.

Half-hour lines = very light dashed lines.

Reduce opacity of dashed lines.

Grid must not visually dominate shifts.

Department Separators:

Add subtle horizontal divider between departments.

Ensure shift cards do not overlap section header.

Z-index Rules:

Grid at lowest layer.

Row background above grid.

Shift cards above row background.

Headers above everything.

Overall:

Clean enterprise look.

Remove visual noise.

Improve contrast hierarchy between grid, rows, and cards.

Do not change functionality — only correct layout structure and alignment.