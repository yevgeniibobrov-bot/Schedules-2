Refine and clean up Day view layout. Do not change structure — only fix layout, spacing and visual hierarchy.

Shift Cards Alignment:

Ensure shift cards are vertically centered within employee rows.

Cards must not overflow row boundaries.

Maintain consistent top/bottom padding inside each row.

Fix cases where cards visually overlap section dividers.

Time Grid:

Hour lines = subtle solid vertical lines.

Half-hour lines = lighter dashed lines (reduce contrast).

Ensure consistent spacing across full width.

Grid must align perfectly with shift start/end times.

Resources Row:

Make Resources background lighter and less visually dominant.

Forecast / Scheduled / Delta values must align to the right side of the row.

Use smaller font for labels, emphasize numeric values.

Reduce red background opacity in deficit state.

Department Headers:

Keep department header background neutral and clean.

Ensure section divider lines are clearly visible but subtle.

Remove unnecessary color noise.

Z-index & Overlap:

Shift cards must always render above grid.

No card should visually merge with another row.

Maintain clear separation between employees.

Spacing Consistency:

Equal row height for all employees.

Equal spacing between departments.

Maintain consistent horizontal padding from left panel.

Visual Polish:

Improve contrast balance between grid and content.

Avoid heavy background tints that compete with shifts.

Keep interface clean and enterprise-grade.

Do not modify functionality — only layout and visual refinement.